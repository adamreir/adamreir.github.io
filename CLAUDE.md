# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

GitHub Pages academic website for Adam Reiremo (`adamreir.github.io`). Currently in early scaffolding — no framework or build system has been chosen yet.

## Architecture

This will be a static site deployed via GitHub Pages. The tech stack (static site generator, CSS framework, etc.) is TBD. Common choices for academic sites include:

- **Jekyll** — GitHub Pages default, no build step needed, Markdown-driven
- **Hugo** — fast builds, Markdown-driven, many academic themes (e.g., Hugo Academic/Wowchemy)
- **Plain HTML/CSS/JS** — no build step, full control

Once a framework is chosen, update this file with build/dev/test commands and the resulting directory structure.

## Deployment

Pushing to `main` automatically deploys via GitHub Pages. The published site is at `https://adamreir.github.io`.
